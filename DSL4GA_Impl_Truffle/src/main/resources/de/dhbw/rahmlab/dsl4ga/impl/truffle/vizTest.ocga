#algebra cga

fn add(a, b) {
	a+b
}

fn dummy(a) {
	a+1
}

fn main(a, b) {
	c[] = {3, 5, 1}
	d[] = {7, 9, 1}
	// e = mapfold(add, 3, c)
	// e[] = concat(c, d)
	e[] = d[:-1]
	// f[] = range(0, 100, 1)

	g[] = map(add, c, d)

	// h = dummy(2)²+5

	p = 15*
	p
}
