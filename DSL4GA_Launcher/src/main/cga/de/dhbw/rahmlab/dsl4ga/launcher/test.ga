fn main(P1, P2, P3, PIc) {
    :L01 := (ε₀^ε₃^εᵢ)*
	:L12 := (P1^P2^εᵢ)*
	:L23 := (P2^P3^εᵢ)*
	:P0 := ε₀ 
	a1 := ε₂
	b1 := -PIc
	N1 := ε₁^ε₂
	x1 := (a1^b1)/N1
	y1 := a1⋅b1;
	// comment
	alpha := atan2(y1,x1)
	L01, L12, L23, alpha
}
